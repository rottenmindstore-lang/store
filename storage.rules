rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Imagens da loja — leitura pública, upload só autenticado
    match /loja/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
